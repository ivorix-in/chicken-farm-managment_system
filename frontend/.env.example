# Backend API base URL.
# Leave empty in development — Vite proxy forwards /api → http://localhost:9000 (see vite.config.ts).
# Set to the full backend URL in production, e.g. https://api.chickenfarmmanagement.com
VITE_API_URL=
